# 三大顶会投稿：实验公平性、严谨性与可复现性要求

> 覆盖会议：**ECML PKDD Applied Data Science Track** · **CIKM Applied Research Papers** · **ICDM (application-oriented)**

---

## 一、各会议官方层面的明确要求

### 1. ECML PKDD — Applied Data Science Track

| 维度 | 要求 |
|------|------|
| **可复现性** | 强烈鼓励（Strongly Encouraged）遵循可复现研究最佳实践 |
| **代码/数据公开** | 鼓励公开；使用标准托管平台并分配 **DOI**（GitHub, Bitbucket, figshare 等用于代码；Zenodo, OpenML, Dataverse 等用于数据集） |
| **专有数据** | 可接受，但**至少部分数据须公开**，或同时使用公开数据集，以保证透明度和可复现性 |
| **TRL 要求** | 最低 **Technology Readiness Level 4**（技术在实验室环境中已验证） |
| **论文描述** | 需清晰说明：真实世界挑战、数据属性（规模/质量）、方法论及其评估程序、结论与领域影响 |
| **伦理声明** | 提交表单中需说明伦理影响（数据收集、个人信息推断等） |
| **GenAI 使用** | 需遵守 European Code of Conduct for Research Integrity，负责任地使用 |

---

### 2. CIKM — Applied Research Papers

| 维度 | 要求 |
|------|------|
| **应用落地** | 论文必须说明系统**已部署或计划部署**、运行时长、以及可量化的实际影响 |
| **代码公开** | 鼓励通过 GitHub、GitLab、Bitbucket 等平台公开；需提供完整文档（功能、依赖、限制） |
| **数据** | 遵循 ACM 伦理标准，数据集须符合机构 IRB 规范 |
| **GenAI 披露** | **强制要求**：在正文页数内、参考文献前设置独立的 **"GenAI Usage Disclosure"** 章节，不计入页数限制；须全面披露在研究（含代码与数据）和写作各阶段对 GenAI 工具的使用情况 |
| **双盲审稿** | 匿名提交，审稿人不可见作者信息 |
| **实际贡献** | 重点考察：系统落地性、可扩展性（Scalability）、可量化收益（Measurable Benefits） |

---

### 3. ICDM — Application-oriented Submission

| 维度 | 要求 |
|------|------|
| **可复现性清单** | **必须**在投稿时完成并提交 [Reproducibility Checklist](https://www.cs.mcgill.ca/~jpineau/ReproducibilityChecklist.pdf)（NeurIPS 标准）；该清单将共享给 Area Chair 和 Reviewer |
| **算法描述** | 算法和所用资源须**尽可能完整描述**，包括实验方法论、实证评估、结果 |
| **代码匿名化** | 若在审稿期间共享代码，**必须匿名化**（如使用 Anonymous GitHub 或 Anonymous OSF），移除姓名、邮箱、机构信息（包括注释、commit message、README） |
| **公开数据集** | **强烈鼓励**在公开数据集上报告结果 |
| **三盲审稿** | **Triple-blind**：审稿人、PC 成员相互匿名，作者身份在接受决定前对所有人保密 |
| **评审权重** | 可复现性评估**将直接影响最终录用决定** |

---

## 二、代码层面的通用要求（跨三会议）

### 2.1 代码质量与结构

```
project/
├── README.md              # 快速开始、环境配置、运行说明
├── requirements.txt       # 依赖库 + 版本号（固定，如 torch==2.1.0）
├── environment.yml        # (可选) Conda 环境文件
├── data/                  # 数据预处理脚本或下载说明
├── src/                   # 核心模型与算法代码
├── experiments/           # 实验配置文件（YAML/JSON）
├── scripts/               # 一键运行脚本（如 run_all.sh）
├── results/               # 输出结果占位（或说明如何生成）
└── LICENSE
```

- **README 必须包含**：环境安装步骤、数据准备说明、训练/推理命令、如何复现论文中每张表/图的具体命令
- **依赖版本锁定**：所有关键库固定版本，避免"在我机器上能跑"的问题
- **随机种子固定**：所有随机性来源（Python `random`、`numpy`、`torch`、`tensorflow`、CUDA）均需显式设置 seed 并在论文中说明
- **注释与文档**：关键函数、核心算法须有清晰注释；参数含义不得依赖"读者猜测"

### 2.2 匿名化要求（投稿期间）

针对 **ICDM（强制）** 和 **ECML PKDD/CIKM（建议）**：

- 使用 [Anonymous GitHub](https://anonymous.4open.science/) 或 [Anonymous OSF](https://osf.io/) 进行审稿期间的匿名代码共享
- 清除以下内容：
  - 代码注释中的姓名/邮箱/机构
  - `git log` commit message 中的身份信息
  - README、配置文件（config.yaml）中的路径（如 `/home/yourname/...`）
  - 模型 checkpoint 文件中的元数据
- 接受后可替换为正式公开仓库（推荐分配 DOI，如通过 Zenodo）

---

## 三、实验设计层面的公平性要求

### 3.1 基线（Baseline）公平对比

| 原则 | 具体做法 |
|------|----------|
| **相同实验条件** | 所有方法在相同数据划分、相同随机种子、相同硬件下运行 |
| **超参调优一致性** | 自己的方法与 baseline 的超参搜索策略须相同（同样的搜索空间和 budget），不可只对自己方法做精细调参 |
| **使用官方实现** | baseline 优先使用**官方开源代码**，并在论文中注明版本/commit hash；若自行重实现，须说明原因并验证其性能与原文一致 |
| **公平的数据划分** | 训练/验证/测试集划分方式须统一，且测试集不得用于任何超参调整 |

### 3.2 数据处理透明性

- **预处理步骤**：完整记录所有数据清洗、归一化、增强步骤，且对所有比较方法一致应用
- **数据泄露检查**：确认特征工程未使用测试集信息（如基于全集的归一化统计量）
- **数据集版本**：明确记录数据集版本号、下载日期、原始来源 URL
- **专有数据**（ECML PKDD 特别要求）：若使用非公开数据，须**额外提供在公开数据集上的实验结果**

### 3.3 评估协议严谨性

- **统计显著性**：多次独立运行（建议 ≥5 次），报告**均值 ± 标准差**；高分差异须附显著性检验（如 t-test, Wilcoxon）
- **评估指标**：选择与任务对齐的标准指标，说明选择理由；避免仅汇报对自己有利的指标
- **消融实验**：完整的 Ablation Study，每个模块贡献须独立验证

---

## 四、可复现性 Checklist（整合 NeurIPS 标准，ICDM 强制，其他会议推荐）

以下内容对应 ICDM 要求提交的 Reproducibility Checklist，同时也是 ECML PKDD 和 CIKM 审稿人会隐性考察的维度：

### A. 模型与方法描述
- [ ] 论文中包含算法伪代码（Pseudocode）或足够详细的方法描述
- [ ] 所有数学符号定义清晰，无歧义
- [ ] 模型的所有假设（Assumptions）明确列出

### B. 实验环境
- [ ] 硬件配置（GPU 型号、内存、CPU）已在论文中说明
- [ ] 操作系统、CUDA 版本、主要框架版本已记录
- [ ] 训练时长、推理时延（如适用）已报告

### C. 数据
- [ ] 数据集来源、版本、访问链接已提供
- [ ] 训练/验证/测试集划分方式已说明
- [ ] 数据预处理步骤完整描述
- [ ] 数据集使用已获得授权（许可协议说明）

### D. 超参数
- [ ] 所有超参数的最终值在论文中列出（可在附录）
- [ ] 超参数搜索方式（Grid Search / Random Search / 手动）已说明
- [ ] 搜索范围（Search Space）已说明

### E. 随机性控制
- [ ] 随机种子已固定并在论文/代码中说明
- [ ] 多次运行的方差已报告

### F. 代码与数据发布
- [ ] 代码托管于公开仓库（附 DOI 或稳定链接）
- [ ] README 包含完整复现说明
- [ ] 代码在干净环境中经过独立测试（如 Docker 或 CI/CD）

---

## 五、各会议关键差异对比

| 维度 | ECML PKDD ADS | CIKM Applied | ICDM |
|------|:---:|:---:|:---:|
| 可复现性是否强制 | 鼓励 | 鼓励 | **强制清单** |
| 代码匿名化要求 | — | 双盲 | **Triple-blind + 匿名化代码** |
| 公开数据集要求 | 至少部分公开 | 建议 | **强烈鼓励** |
| GenAI 披露 | 需声明 | **独立章节，强制** | 需声明 |
| 应用落地证明 | TRL ≥ 4 | **已部署/计划部署** | 不单独要求 |
| 专有数据处理 | 需配合公开数据集 | IRB 合规 | 鼓励公开 |
| 可复现性影响录用 | 是（隐性） | 是（隐性） | **是（显性权重）** |

---

## 六、实用建议

1. **一套代码，三处提交**：维护一个规范的主仓库，根据会议要求切换匿名/公开状态，避免维护多套代码。

2. **提前 Docker 化**：在 Docker 容器中验证整个实验流程可从零复现，能有效防止环境依赖问题，同时也是向审稿人展示严谨性的最强信号。

3. **结果表格自动生成**：用脚本直接从实验日志生成论文中的表格，避免手动复制导致的数字错误，同时使结果完全可追溯。

4. **DOI 分配时机**：在论文 camera-ready 阶段通过 Zenodo 将 GitHub 仓库打包归档并分配 DOI，满足 ECML PKDD 的持久化要求（GitHub URL 本身不算稳定引用）。

5. **ICDM 的 Checklist 优先处理**：NeurIPS Reproducibility Checklist 涵盖范围最广，若先按其完成代码和实验，则 ECML PKDD 和 CIKM 的要求基本都会顺带满足。
